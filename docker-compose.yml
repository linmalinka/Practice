
services:
    server:
        build: .
        command: python3 http_local_server.py
        ports:
            - 8000:8000

    client:
        build: .
        command: sh -c "sleep 3 && python3 http_client.py"

        depends_on:
            - server
